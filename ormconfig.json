{
  "type": "postgres",
  "port": 5432,
  "host": "localhost",
  "name": "default",
  "username": "docker",
  "password": "ignite",
  "database": "fin_api",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
